# 도로-교각 통합 능동 진동 제어 연구
### IoT 액추에이터 팀프로젝트 계획서

---

## 1. 프로젝트 개요

- **주제**: 도로 매립형 진동 상쇄 시스템(AVC) 연구 후, 교각 상단 능동질량감쇠기(AMD)를 확장 연구로 추가하는 통합 진동제어 연구
- **목표**: IoT 센서·액추에이터 기반 능동 진동 제어를 통해 (1) 도로 내구성 향상 (2) 차량 주행성(승차감) 향상 (3) 교각 구조 안전성 향상을 정량적으로 검증
- **연구 구조**:
  - **1단계 (핵심 연구)**: 도로 상판 하부에 매립된 액추에이터가 차량 통행 진동을 실시간 감지·상쇄 (노이즈캔슬링 원리)
  - **2단계 (확장 연구)**: 교각이 길어질 때 발생하는 바람·지진에 의한 수평 흔들림을 상단 무게추(AMD)로 감쇠 (고층건물 제진 원리)
- **기간**: 1학기(8~12주)
- **팀 조건**: 코딩 경험 있음 / ROS 미경험 / 시뮬레이션 중심, 실물은 축소 모형으로 최소 검증
- **사용 도구**: MATLAB/Simulink(모델링·제어설계), 아두이노(실물 제어), 가속도센서, 액추에이터(솔레노이드/보이스코일/모터)

## 2. 배경 및 문제제기

- 고속도로 교량은 성격이 다른 두 진동원에 노출된다.
  1. **차량 통행에 의한 노면(상판) 진동**: 반복적·국소적, 차량 승차감과 도로 피로 손상에 직접 영향
  2. **바람·지진에 의한 교각 전체의 수평 흔들림(sway)**: 교각이 길어질수록(고교각) 구조적으로 취약해지며, 고층건물이 높아질수록 풍하중에 취약해지는 것과 동일한 원리
- 반복 진동은 구조물의 **피로(fatigue)를 누적**시켜 내구연한을 단축시키고, 동시에 노면 진동은 차량 서스펜션에 전달되어 **승차감·주행 안전성**을 저하시킴
- 두 문제는 성격(진동원, 방향, 주파수 대역)이 다르므로 **하나의 시스템이 아닌 두 개의 독립적 서브시스템으로 순차 연구 후 통합**하는 접근이 타당함
  - 1단계: 노면 매립 액추에이터 → 노이즈캔슬링처럼 반대 위상 진동 발생 (AVC, Active Vibration Cancellation)
  - 2단계: 교각 상단 무게추 → 모터로 능동 구동해 수평 흔들림 상쇄 (AMD, Active Mass Damper)
  - 수동형 TMD(스프링+댐퍼만 사용)는 액추에이터가 없어 과제 요건(IoT 액추에이터 활용)에 부합하지 않으므로, 모터 기반 능동 구동 방식(AMD)을 채택

## 3. 물리 모델

### 3.1 기본 1자유도(1-DOF) 진동 시스템 — 1단계(노면 AVC) 기반

**운동방정식 (강제진동 + 액추에이터 제어력)**
```
m·x'' + c·x' + k·x = F(t) - Fa(t)
```
- `m`: 질량, `c`: 감쇠계수, `k`: 강성, `F(t)`: 외란(차량 하중), `Fa(t)`: 액추에이터 제어력

**고유각진동수 / 감쇠비 / 감쇠 고유진동수**
```
ωn = √(k/m)
ζ  = c / (2√(km))
ωd = ωn·√(1-ζ²)
```

### 3.2 자유진동 실험을 통한 m, c, k 파라미터 추정

**로그감쇠율 (Logarithmic Decrement)**
```
δ = (1/n)·ln(x1 / x(n+1))
ζ = δ / √(4π² + δ²)
```
- 실물 구조물을 임펄스 타격 후 감쇠 곡선을 측정해 `ζ`를 구하고, 진동 주기 `T`로부터 `ωn = 2π/T`, `k = m·ωn²`, `c = 2ζ√(km)`를 역산

### 3.3 이동하중 함수 — 차량 통행 재현

**단순 이동점하중**
```
F(t) = P   (차량이 해당 지점 위에 있는 구간, 0 ≤ t ≤ L/v)
F(t) = 0   (그 외)
```

**가우시안 펄스 근사 (Simulink 구현 용이)**
```
F(t) = P · exp[-(t-t0)² / (2σ²)]
```
- `P`: 차량 하중, `L`: 상판 길이, `v`: 차량 속도, `t0`: 하중 통과 시점

### 3.4 안티페이즈(노이즈캔슬링식) 제어 목표 — 1단계 핵심

```
Fa(t) ≈ -A·sin(ωt + φ + π)
```

**적응형 LMS 필터 (실시간 위상·크기 추정)**
```
Fa(t) = Σ wi·x(t-iΔt)
wi(t+1) = wi(t) + μ·e(t)·x(t-iΔt)
```
- `e(t)`: 잔류 진동(오차), `μ`: 학습률. 초기 구현은 아래 PID로 대체 가능

### 3.5 PID 제어 (우선 구현 권장)
```
Fa(t) = Kp·e(t) + Ki·∫e(t)dt + Kd·(de(t)/dt)
```

### 3.6 2자유도(2-DOF) 시스템 — 2단계(교각 AMD) 기반

```
교각 본체:   m1·x1'' + c1·x1' + k1·x1 = F(t) - Fa(t)
AMD 무게추:  m2·x2'' + c2·x2' + k2·(x2-x1) = Fa(t)
```
- `F(t)`: 풍하중 또는 지진파 외란, `Fa(t)`: 액추에이터가 무게추에 가하는 힘 (구조물엔 반작용)

### 3.7 상태공간 모델 및 LQR (2단계 고도화, 선택)

```
X = [x, x']ᵀ
X' = A·X + B·Fa
A = [0, 1; -k/m, -c/m]     B = [0; 1/m]
```

**LQR 비용함수**
```
J = ∫(Xᵀ·Q·X + Fa²·R) dt
```
- MATLAB `K = lqr(A, B, Q, R)` → `Fa = -K·X`

### 3.8 전달함수 및 진동 전달률 (주파수응답 분석)

```
X(s)/F(s) = 1 / (m·s² + c·s + k)

TR(ω) = √[1+(2ζr)²] / √[(1-r²)²+(2ζr)²]     (r = ω/ωn)
```

## 4. 평가 지표

### 4.1 1단계 (노면 AVC) — 도로 내구성·주행성 관점

| 지표 | 계산식/설명 |
|---|---|
| 진폭 저감률 | (A_before - A_after) / A_before × 100 (%) |
| 진동 전달률(Transmissibility) | 3.8절 TR(ω) 식, 차량 전달 진동 감소량 |
| 응답 지연시간 | 진동 감지~액추에이터 개입까지 시간(ms) |
| 등가 피로하중 감소 | 수명비 ≈ (σ_before/σ_after)^m (m: 재료 피로지수, 통상 3~5) |
| 시뮬레이션-실측 오차율 | 시뮬레이션 예측값과 실측값 비교 |

### 4.2 2단계 (교각 AMD) — 구조 안전성 관점

| 지표 | 계산식/설명 |
|---|---|
| 수평 진동(흔들림) 저감률 | (A_before - A_after) / A_before × 100 (%) |
| 정착시간(Settling Time) | Ts ≈ 4 / (ζ·ωn) (근사식, ±2% 기준) |
| 교각 길이별 흔들림 민감도 | 짧은 교각 vs 긴 교각 비교 |
| 시뮬레이션-실측 오차율 | 시뮬레이션 예측값과 실측값 비교 |

### 4.3 통합 검증 지표

| 검증 항목 | 확인 내용 |
|---|---|
| 독립성 | AVC와 AMD가 서로의 제어에 간섭하지 않는지 |
| 동시 작동 안정성 | 두 시스템 동시 작동 시 발산 없이 안정적인지 |
| 종합 성능 | 노면 진동 저감 + 교각 흔들림 저감 동시 달성 여부 |

## 5. 특이사항 및 참고

- **"완전 상쇄"는 이론적으로도 어려움**: 반응 지연(latency)과 위상 오차가 항상 존재하므로, 목표는 "완전 제거"가 아닌 **정량적 진폭 저감률(예: 60~80%)**로 설정하는 것이 현실적
- **1단계와 2단계는 타겟 주파수 대역이 다름**: 1단계는 국소·고주파(차량 통행), 2단계는 전역·저주파(바람/지진) → 제어 게인과 필터 대역을 별도로 설계해야 하며, 통합 시 상호간섭 여부를 별도 검증 필요
- **수동 TMD vs 능동 AMD 구분 명확히 할 것**: 기존 TMD 연구는 스프링+댐퍼 기반 수동 장치가 많으므로, 본 프로젝트에서는 "모터로 무게추를 능동 구동"한다는 점을 차별점으로 강조
- **액추에이터 힘 부족 가능성**: 소형 액추에이터로는 큰 구조물의 진동을 상쇄하기 어려울 수 있어, 모형을 가볍고 소형으로 설계하고 액추에이터 사양은 여유 있게 선정
- **파라미터(m, c, k) 추정 오차 관리**: 자유진동 실험을 여러 번 반복해 평균값을 사용하고, 초기 캘리브레이션을 철저히 할 것
- **참고 실제 사례**: 
  - 능동/수동 제진: 런던 밀레니엄 브릿지(보행교 횡진동 저감), 타이베이101(TMD)
  - 이동하중 문제: 교량공학·구조동역학 분야에서 표준적으로 다뤄지는 주제로 참고문헌 확보 용이

## 6. 로드맵 (요약)

| 주차 | 내용 |
|---|---|
| 1~2주 | 이론 학습(AVC/AMD/TMD, 구조동역학, 이동하중), 스코프 확정, 팀 역할 분담 |
| 3~5주 | [1단계] 노면 모형 제작, 센서/액추에이터 매립, 자유진동 실험으로 m,c,k 추정, Simulink 개방루프 모델 구축 |
| 6~7주 | [1단계] PID 기반 안티페이즈 제어 설계 및 Simulink 폐루프 시뮬레이션 |
| 8주 | [1단계] 실물 폐루프 실험(AVC ON/OFF), 시뮬레이션-실측 비교 분석 |
| 9~10주 | [2단계] 교각 확장 모형 제작, AMD 무게추/액추에이터 장착, 풍하중/지진파 가진 실험, 제어기 설계 및 2-DOF 시뮬레이션 |
| 11주 | 1단계+2단계 통합 검증(복합 시나리오), 전체 지표 종합 정리 |
| 12주 | 보고서 작성, 시각자료 제작, 발표 리허설 및 최종 발표 |

---

### 다음 단계 제안
1. 1단계 자유진동 실험 설계 및 m, c, k 추정 상세 가이드
2. Simulink 1단계(AVC) 블록다이어그램 초안
3. 아두이노 안티페이즈/PID 제어 코드 초안
4. 2단계(AMD) 상세 설계는 1단계 완료 후 진행
5. 보고서 목차 및 발표 슬라이드 구조 초안
