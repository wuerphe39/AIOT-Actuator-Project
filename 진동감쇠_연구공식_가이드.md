# 진동 감쇠 연구 공식 가이드
## 처음 배우는 사람을 위한 상세 설명판

---

## 이 문서를 보는 법

이 문서는 시뮬레이션(MATLAB/Simulink) 모델링과 실험 데이터 분석에 필요한 공식들을, **왜 이 식이 필요한지 → 각 기호가 뭘 의미하는지 → 어떻게 쓰이는지** 순서로 하나씩 풀어서 설명합니다. 전공 수업에서 다루지 않은 개념이 많으니, 급하게 외우기보다 "이 식이 해결하려는 문제가 뭔가"를 먼저 이해하는 방향으로 읽는 걸 권장합니다.

전체 흐름은 이렇습니다:

```
① 진동을 수학으로 표현하기 (운동방정식)
   ↓
② 그 진동의 "성격"을 숫자로 나타내기 (고유진동수, 감쇠비)
   ↓
③ 실물 구조물의 성격(m,c,k)을 실험으로 알아내기 (로그감쇠율)
   ↓
④ 진동을 일으키는 외력을 수식으로 만들기 (이동하중)
   ↓
⑤ 진동을 상쇄하는 제어력을 계산하기 (PID, 안티페이즈, LMS, LQR)
   ↓
⑥ 결과를 정량적으로 평가하기 (저감률, 정착시간, 전달률)
```

---

## 1. 진동을 수학으로 표현하기: 운동방정식

### 1.1 기본 아이디어

모든 물체의 움직임은 뉴턴의 운동법칙 `F = ma`(힘 = 질량×가속도)를 따릅니다. 진동하는 구조물(다리, 도로, 건물 등)도 예외가 아닙니다. 다만 구조물에는 힘이 여러 종류 동시에 작용하기 때문에, 이걸 모두 더한 식을 세워야 합니다.

### 1.2 공식

```
m·x'' + c·x' + k·x = F(t) - Fa(t)
```

### 1.3 기호 설명

| 기호 | 의미 | 비유 |
|---|---|---|
| `x` | 변위 (구조물이 원래 위치에서 얼마나 움직였는지) | 그네가 얼마나 앞뒤로 벌어졌는지 |
| `x'` | 속도 (변위를 시간으로 미분한 것) | 그네가 얼마나 빠르게 움직이는지 |
| `x''` | 가속도 (속도를 시간으로 미분한 것) | 그네의 속도가 얼마나 빨리 변하는지 |
| `m` | 질량 | 그네에 탄 사람의 몸무게 |
| `c` | 감쇠계수 | 공기저항, 마찰처럼 움직임을 방해해서 에너지를 없애는 정도 |
| `k` | 강성(스프링 상수) | 그넷줄이 얼마나 뻣뻣한지, 원위치로 되돌리려는 힘의 세기 |
| `F(t)` | 외력(외란) | 누군가 그네를 미는 힘 (우리가 통제할 수 없는 힘) |
| `Fa(t)` | 액추에이터 제어력 | 우리가 설계해서 반대로 밀어주는 힘 |

### 1.4 왜 이렇게 생겼나 (직관적 이해)

왼쪽 세 항은 물체에 "자연적으로" 작용하는 세 가지 힘입니다.

- **`m·x''`**: 물체를 가속시키려면 힘이 필요하다는 뜻. 무거울수록(m이 클수록) 같은 가속도를 내는 데 더 큰 힘이 필요합니다.
- **`c·x'`**: 속도에 비례해서 운동을 방해하는 힘. 빨리 움직일수록 저항이 커집니다 (수영장에서 빨리 움직이면 물의 저항이 더 세지는 것과 같은 원리).
- **`k·x`**: 변위에 비례해서 원위치로 되돌리려는 힘. 스프링을 많이 늘릴수록 다시 당기는 힘도 커집니다.

오른쪽은 "밖에서 가해지는 힘"입니다. `F(t)`는 우리가 막을 수 없는 힘(차량 하중, 바람)이고, `Fa(t)`는 우리가 설계하는 액추에이터의 힘입니다.

### 1.5 이 식으로 뭘 하는가

이 식을 컴퓨터(Simulink)에 넣고 시간에 따라 풀면(적분하면), `x(t)` — 즉 "시간이 지나면서 구조물이 어떻게 움직이는지" — 를 알 수 있습니다. 시뮬레이션의 본질은 결국 이 미분방정식을 푸는 것입니다.

---

## 2. 진동의 "성격"을 숫자로 나타내기

같은 운동방정식이라도 `m, c, k` 값에 따라 진동이 빠르게 진동하는지, 느리게 진동하는지, 빨리 멈추는지 오래 지속되는지가 달라집니다. 이 성격을 요약하는 세 가지 숫자가 있습니다.

### 2.1 고유각진동수 (Natural Frequency)

```
ωn = √(k/m)
```

**의미**: 만약 감쇠(c)가 전혀 없다고 가정하면, 이 구조물은 외력이 없어도 스스로 초당 `ωn`(단위: rad/s, 라디안/초)의 속도로 영원히 진동합니다. 이건 그 구조물만의 "타고난 리듬"이라고 생각하면 됩니다.

**직관**: `k`(뻣뻣함)가 클수록 빨리 진동하고, `m`(무게)이 클수록 느리게 진동합니다. 기타 줄을 팽팽하게 조이면(k 증가) 높은 음(빠른 진동)이 나고, 굵은 줄(m 증가)은 낮은 음(느린 진동)이 나는 것과 같은 원리입니다.

### 2.2 감쇠비 (Damping Ratio)

```
ζ = c / (2√(km))
```

**의미**: 이 시스템이 "얼마나 잘 멈추는지"를 나타내는 무차원(단위 없는) 숫자입니다. 값의 범위에 따라 세 가지로 나뉩니다.

| 범위 | 이름 | 움직임 |
|---|---|---|
| ζ = 0 | 무감쇠 | 영원히 진동 (비현실적, 이론적 극한값) |
| 0 < ζ < 1 | **부족감쇠 (underdamped)** | 진동하면서 서서히 잦아듦 — **우리가 다루는 대부분의 실제 구조물** |
| ζ = 1 | 임계감쇠 | 진동 없이 가장 빠르게 원위치로 복귀 |
| ζ > 1 | 과감쇠 | 진동은 없지만 복귀가 느림 (예: 문이 천천히 닫히는 도어클로저) |

**이 프로젝트에서 중요한 이유**: 액추에이터의 목표는 결국 이 `ζ` 값을 인위적으로 키우는 것과 같습니다. 감쇠비가 클수록 진동이 빨리 멈추기 때문에, "능동 제어로 감쇠비를 인위적으로 증가시킨다"는 게 이 연구 전체의 핵심 아이디어입니다.

### 2.3 감쇠 고유진동수 (Damped Natural Frequency)

```
ωd = ωn·√(1-ζ²)
```

**의미**: 실제로 감쇠가 있는 상태에서 구조물이 진동하는 "진짜" 주파수입니다. 감쇠가 진동을 살짝 느리게 만들기 때문에, `ωd`는 항상 `ωn`보다 약간 작습니다.

**실험에서 왜 중요한가**: 실물 구조물을 실제로 흔들어서 진동 주기를 측정하면, 그 값이 이론상 `ωn`이 아니라 실제로는 `ωd`입니다. 나중에 3장에서 이 값을 이용해 `m, c, k`를 역산할 때 이 구분이 중요해집니다.

---

## 3. 실물 구조물의 m, c, k를 실험으로 알아내기

### 3.1 문제 상황

시뮬레이션을 하려면 `m, c, k` 값이 필요한데, 우리가 직접 만든 모형은 정확한 설계도가 없으니 이 값을 이론적으로 계산하기 어렵습니다. 그래서 **실험으로 거꾸로 알아내는** 방법이 필요합니다.

### 3.2 실험 방법 (개념)

구조물을 손으로 툭 쳐서(임펄스를 가해서) 자유진동시키면, 진폭이 `x1 → x2 → x3 → ...`로 점점 작아집니다. 부족감쇠 시스템(2.2절)에서는 이 진폭이 **매 주기마다 일정한 비율로 줄어드는** 특별한 성질이 있습니다 (지수함수적 감소).

### 3.3 로그감쇠율 (Logarithmic Decrement) 공식

```
δ = (1/n)·ln(x1/x(n+1))
```

**기호 설명**:
- `x1`: 첫 번째로 측정한 진폭
- `x(n+1)`: `n`번 주기가 지난 뒤의 진폭
- `ln`: 자연로그
- `δ` (델타): 로그감쇠율 — "한 주기마다 진폭이 로그 스케일로 얼마나 줄어드는가"

**왜 여러 주기(n)를 쓰는가**: 한 주기만 보면 측정 노이즈에 민감하니, 여러 주기에 걸쳐 평균을 냄으로써 더 안정적인 값을 얻습니다.

### 3.4 로그감쇠율 → 감쇠비 변환

```
ζ = δ/√(4π²+δ²)
```

이 식은 3.3에서 구한 `δ`를 2.2절에서 정의한 감쇠비 `ζ`로 바꿔주는 변환식입니다. (유도 과정은 부족감쇠 진동의 지수함수 해로부터 나오는데, 결과 공식만 알고 사용해도 무방합니다.)

### 3.5 나머지 값 역산하기

`ζ`를 구했으면, 실험에서 측정한 진동 주기 `T`를 이용해 순서대로:

```
ωn = 2π/T          (측정된 진동 주기로부터 고유각진동수 계산)
k  = m·ωn²          (질량 m은 저울로 직접 측정한 값 사용)
c  = 2ζ√(km)         (2.2절 공식을 c에 대해 풀어서 역산)
```

이렇게 하면 시뮬레이션에 넣을 `m, c, k` 세 값을 모두 확보하게 됩니다. `m`은 실제로 저울로 재는 게 계산으로 구하는 것보다 정확합니다.

---

## 4. 진동을 일으키는 외력을 수식으로 만들기: 이동하중

### 4.1 문제 상황

차량이 도로 상판 위를 지나갈 때 특정 지점에 가해지는 힘 `F(t)`는 시간에 따라 변합니다(차가 다가올 때 커지고, 지나가면 사라짐). 이걸 수식으로 표현해야 시뮬레이션에 입력할 수 있습니다.

### 4.2 단순 모델 (네모파)

```
F(t) = P   (차량이 그 지점 위에 있는 동안, 즉 0 ≤ t ≤ L/v)
F(t) = 0   (그 외 시간)
```

**기호**: `P`는 차량 하중(무게), `L`은 상판 길이, `v`는 차량 속도.

**한계**: 힘이 갑자기 켜졌다가 갑자기 꺼지는 부자연스러운 형태입니다.

### 4.3 가우시안 펄스 모델 (권장)

```
F(t) = P·exp[-(t-t0)²/(2σ²)]
```

**기호 설명**:
- `exp[...]`: 자연상수 e의 거듭제곱 (정규분포 곡선을 만드는 함수)
- `t0`: 하중이 최대가 되는 시각 (차량이 정확히 그 지점을 지나는 순간)
- `σ` (시그마): 하중이 얼마나 넓게 퍼져서 작용하는지 (통계학의 "표준편차"와 같은 역할)

**직관**: 이 식은 정규분포(종 모양 곡선)와 똑같이 생겼습니다. `t0`를 중심으로 좌우로 서서히 커졌다가 작아지는 모양이라 실제 차량 통과 상황과 더 비슷하고, 수학적으로 매끄러워서(미분 가능) Simulink에서 다루기도 편합니다. 차량 속도 `v`가 빠를수록 `σ`는 작아집니다(짧은 시간에 스쳐 지나가므로).

---

## 5. 진동을 상쇄하는 제어력 계산하기

여기서부터가 이 프로젝트의 핵심 — "액추에이터가 얼마만큼의 힘 `Fa(t)`을 내야 하는가"를 결정하는 알고리즘들입니다.

### 5.1 안티페이즈(노이즈캔슬링) 제어 목표

```
Fa(t) ≈ -A·sin(ωt+φ+π)
```

**아이디어**: 만약 구조물의 진동이 `A·sin(ωt+φ)`라는 사인파(진폭 A, 각속도 ω, 위상 φ)라면, 이걸 없애는 가장 간단한 방법은 **같은 크기·같은 주파수지만 위상이 정확히 180˚(π 라디안) 반대인 파동**을 더하는 것입니다.

**수학적으로 왜 상쇄되는가**: 삼각함수 성질 `sin(θ+π) = -sin(θ)`을 이용하면,

```
A·sin(ωt+φ) + [-A·sin(ωt+φ)] = 0
```

두 파동을 더하면 이론적으로 완전히 0이 됩니다. 이게 노이즈캔슬링 헤드폰이 소음을 없애는 것과 똑같은 원리입니다.

**현실적 한계**: 실제 진동은 순수한 사인파가 아니라 여러 주파수가 섞여 있고, 센서가 진동을 감지한 순간부터 액추에이터가 반응하기까지 시간차(지연, latency)가 있어서 완벽한 상쇄는 어렵습니다. 그래서 목표를 "완전 제거"가 아니라 "저감률(예: 60~80% 감소)"로 현실화하는 것이 일반적입니다.

### 5.2 LMS 적응 필터 (안티페이즈를 실제로 구현하는 방법)

```
Fa(t) = Σ wi·x(t-iΔt)
wi(t+1) = wi(t) + μ·e(t)·x(t-iΔt)
```

**왜 필요한가**: 5.1은 진동이 "순수한 사인파"라고 가정했지만, 실제로는 그렇지 않습니다. 그래서 "과거 진동 데이터를 보고, 지금 얼마나 반대 힘을 줘야 할지 자동으로 학습"하는 방법이 필요합니다.

**첫 번째 식 (제어력 계산)**:
- `x(t-iΔt)`: 조금 전, 더 조금 전... 과거 시점들의 진동값 (`i`가 0, 1, 2, ...로 커질수록 더 과거)
- `wi`: 각 과거 값에 곱해지는 가중치. "이 시점의 진동이 지금 얼마나 중요한 영향을 주는지"를 나타냄
- 이 가중치들을 곱해서 다 더한 값이 지금 내야 할 액추에이터 힘

**두 번째 식 (가중치 업데이트, 학습 과정)**:
- `e(t)`: 잔류 진동(오차) — 상쇄가 잘 안 돼서 남아있는 진동의 크기
- `μ` (뮤): 학습률 — 한 번에 얼마나 크게 가중치를 조정할지
- 오차가 크면, 그 오차와 관련 있는 과거 진동값 방향으로 가중치를 조금씩 조정합니다. 이 과정을 반복하면 가중치가 점점 "최적의 반대파형"을 만드는 방향으로 수렴합니다.

**비유**: 다트를 던지는데, 빗나간 방향과 정도를 보고 다음번 던지는 각도를 조금씩 수정해나가는 것과 비슷합니다. `μ`가 크면 빨리 배우지만 오히려 불안정해질 수 있고(수정을 너무 크게 함), 작으면 안정적이지만 배우는 속도가 느립니다.

**구현 난이도**: 이 방식이 이론적으로는 가장 정교하지만 구현이 복잡하므로, 먼저 아래 5.3(PID)으로 시작하는 것을 권장합니다.

### 5.3 PID 제어 (가장 기본, 우선 구현 권장)

```
Fa(t) = Kp·e(t) + Ki·∫e(t)dt + Kd·(de(t)/dt)
```

**배경**: 산업 전반에서 가장 널리 쓰이는 제어 방식으로, 세 가지 성분을 더해서 제어력을 만듭니다. `e(t)`는 오차(목표 변위 - 현재 변위. 진동 제어에서는 보통 목표 변위가 0)입니다.

| 항 | 이름 | 역할 | 비유 |
|---|---|---|---|
| `Kp·e(t)` | 비례항(P) | 오차가 클수록 힘도 크게 | 많이 벗어났으면 세게 밀어준다 |
| `Ki·∫e(t)dt` | 적분항(I) | 오차가 오래 누적되면 힘을 점점 키움 | "계속 조금씩 어긋나 있었네, 더 세게 밀어야겠다" |
| `Kd·(de(t)/dt)` | 미분항(D) | 오차가 변하는 속도를 보고 미리 대응 | 급하게 다가오면 미리 브레이크를 밟는다 |

**진동 제어에서 특히 중요한 점**: `Kd`(미분항)는 "속도"에 반응하는 성질이 있어서, 사실상 2장에서 배운 감쇠계수 `c`를 인위적으로 늘려주는 것과 비슷한 효과를 냅니다. 그래서 진동 제어에서는 미분항의 역할이 큽니다.

**게인 튜닝**: `Kp, Ki, Kd` 세 값을 얼마로 설정하느냐가 성능을 좌우합니다. Simulink의 PID Tuner 기능을 쓰면 자동으로 적절한 값을 추천받을 수 있습니다.

---

## 6. 두 개의 질량이 연결된 경우: 2자유도(2-DOF) 시스템

교각 본체 위에 별도의 무게추(AMD)를 얹는 것처럼, 두 개의 질량이 서로 연결되어 있는 경우 1장의 식을 확장해야 합니다.

### 6.1 공식

```
구조물:    m1·x1'' + c1·x1' + k1·x1 = F(t) - Fa(t)
AMD 질량:  m2·x2'' + c2·x2' + k2·(x2-x1) = Fa(t)
```

### 6.2 1장과 달라진 점

- 아래첨자 1은 구조물(교각) 본체, 2는 위에 얹힌 무게추를 나타냅니다.
- 핵심은 두 번째 식의 `k2·(x2-x1)` 부분입니다. 무게추의 스프링(또는 지지구조)은 "무게추 자신의 절대적인 위치"가 아니라 **"구조물과 무게추 사이의 상대적인 위치 차이"**에 반응합니다. 무게추가 구조물 위에 얹혀서 상대적으로 움직이는 구조이기 때문입니다.
- 액추에이터 힘 `Fa(t)`는 무게추에는 그대로(+ 부호), 구조물에는 반작용(- 부호)으로 작용합니다. 이건 뉴턴의 제3법칙(작용-반작용)입니다 — 무게추를 한쪽으로 밀면, 그 반작용으로 구조물은 반대쪽으로 밀립니다. 이 반작용의 힘이 바로 외력 `F(t)`(바람, 지진 등)로 인한 흔들림을 상쇄하는 원리입니다.

**비유**: 배 위에서 한쪽으로 발을 세게 구르면 배는 반대쪽으로 살짝 밀립니다. 무게추를 빠르게 움직이는 것도 같은 원리로 구조물에 반작용을 만듭니다.

---

## 7. 더 똑똑한 제어: 상태공간 모델과 LQR (심화, 선택)

### 7.1 상태공간 모델로 바꾸는 이유

1장의 운동방정식은 `x''`(2차 미분)이 포함된 **2차 미분방정식**입니다. 컴퓨터가 다루기 편하려면 **1차 미분방정식 여러 개**로 바꾸는 게 유리합니다. 이렇게 형태를 바꾼 것이 "상태공간(state-space) 모델"입니다.

### 7.2 공식

```
상태벡터: X = [x, x']ᵀ

X' = A·X + B·Fa

A = [0        1    ]        B = [0  ]
    [-k/m   -c/m  ]              [1/m]
```

**설명**: 변위 `x`와 속도 `x'`를 하나의 묶음(벡터) `X`로 만들고, 이 묶음이 시간에 따라 어떻게 변하는지(`X'`)를 행렬 `A`, `B`로 표현합니다. 이렇게 바꾸면 MATLAB의 다양한 제어이론 함수(`lqr`, `ss`, `bode` 등)를 바로 사용할 수 있다는 실용적 장점이 있습니다.

### 7.3 LQR (Linear Quadratic Regulator) 비용함수

```
J = ∫(Xᵀ·Q·X + Fa²·R) dt
```

**아이디어**: PID는 게인(Kp, Ki, Kd)을 사람이 직접 시행착오로 찾아야 하지만, LQR은 "무엇을 중요하게 여길지"만 정해주면 컴퓨터가 수학적으로 최적의 게인을 계산해줍니다.

**기호 설명**:
- `Q`: 진동(오차)을 얼마나 중요하게 여길지 정하는 가중치 행렬. `Q`가 크면 → 진동을 빨리 줄이는 것을 우선시
- `R`: 제어력(에너지 소모)을 얼마나 아낄지 정하는 가중치. `R`이 클수록 → 액추에이터 힘을 아끼는 것을 우선시
- `J`: 이 두 가지(진동 크기 + 에너지 소모)를 합친 총 "비용". LQR은 이 비용을 최소화하는 방법을 찾습니다

**사용법**: MATLAB에서 `K = lqr(A, B, Q, R)` 한 줄이면 최적 게인 행렬 `K`가 계산되고, 이후 `Fa = -K·X`로 제어력을 구합니다. PID처럼 게인을 수동으로 튜닝하지 않아도 되는 게 큰 장점입니다.

---

## 8. 주파수 관점에서 분석하기: 전달함수와 전달률

### 8.1 전달함수 (Transfer Function)

```
X(s)/F(s) = 1/(ms²+cs+k)
```

**의미**: 1장의 운동방정식을 라플라스 변환(시간 영역의 미분방정식을 주파수 영역의 대수식으로 바꾸는 수학적 도구)한 것입니다. "입력 힘 `F`를 넣으면 출력 변위 `X`가 얼마나 나오는가"를 하나의 함수로 요약한 것으로, 이걸 이용하면 시간에 따른 복잡한 계산 없이 **주파수별 반응을 빠르게 분석**할 수 있습니다.

### 8.2 진동 전달률 (Transmissibility)

```
TR(ω) = √[1+(2ζr)²] / √[(1-r²)²+(2ζr)²]      (단, r = ω/ωn)
```

**의미**: 승차감 평가에 직접 쓰이는 지표입니다. `r`은 "지금 가해지는 진동의 주파수(ω)가 구조물의 고유진동수(ωn)의 몇 배인지"를 나타냅니다.

**해석 기준**:
- `TR > 1`: 진동이 오히려 증폭됨 (특히 `r ≈ 1`, 즉 가진 주파수가 고유진동수와 비슷할 때 = 공진 상태로 매우 위험)
- `TR < 1`: 진동이 줄어들어서 전달됨 (우리가 원하는 상태)
- `TR = 1`: 진동이 그대로 전달됨

**이 프로젝트에서의 의미**: 이 값을 "도로 상판에서 차량으로 전달되는 진동의 비율"이라고 생각하면, `TR`을 낮추는 것이 곧 "승차감 개선"과 직결됩니다. 어느 주파수 대역(`r` 범위)에서 우리 시스템의 `TR`이 특히 작게 나오는지 분석하면, "이 제어 시스템은 어떤 속도로 달리는 차량(=어떤 주파수)에 특히 효과적인지"를 설명할 수 있습니다.

---

## 9. 결과를 정량적으로 평가하기

### 9.1 진폭 저감률

```
저감률(%) = (A_before - A_after) / A_before × 100
```

**의미**: 제어를 켜기 전(A_before)과 켠 후(A_after)의 진동 진폭을 비교해서, 몇 퍼센트나 줄었는지 계산합니다. 가장 직관적이고 발표에서 강조하기 좋은 지표입니다.

### 9.2 정착시간 (Settling Time)

```
Ts ≈ 4/(ζ·ωn)     (진폭이 ±2% 범위 이내로 줄어드는 데 걸리는 시간, 근사식)
```

**의미**: 진동이 시작된 후 "충분히 잦아들었다"고 볼 수 있는 범위(보통 초기 진폭의 2% 이내)에 도달하기까지 걸리는 시간입니다. 이 식은 2장에서 배운 감쇠비 `ζ`와 고유진동수 `ωn`으로부터 근사적으로 계산할 수 있습니다. `ζ`가 클수록(감쇠가 강할수록) 정착시간이 짧아진다는 걸 식에서 바로 확인할 수 있습니다.

### 9.3 등가 피로하중 감소 (구조 내구성 지표)

```
수명비 ≈ (σ_before/σ_after)^m     (m: 재료별 피로지수, 통상 3~5)
```

**배경 지식**: 재료공학에서는 반복되는 응력(스트레스)의 크기가 조금만 줄어도 재료가 버틸 수 있는 반복 횟수(피로수명)는 훨씬 크게 늘어난다는 게 잘 알려져 있습니다(S-N 곡선이라는 실험적 관계에 기반).

**기호 설명**:
- `σ_before, σ_after`: 제어 전/후의 응력 진폭(반복 하중의 크기)
- `m`: 재료마다 다른 피로 민감도를 나타내는 지수(보통 강철 계열은 3~5 사이 값 사용)
- 예를 들어 응력이 20% 줄었다면(σ_after/σ_before = 0.8), `m=4`일 때 수명은 `(1/0.8)^4 ≈ 2.44배`로 늘어난다는 계산이 나옵니다

**이 프로젝트에서의 의미**: "진동을 줄이면 도로/구조물이 더 오래 버틴다"는 주장을 정량적 숫자로 뒷받침하는 지표입니다. 다만 실제 `m` 값과 정확한 응력 측정은 재료시험 수준의 정밀도가 필요하므로, 발표에서는 "개념적 추정치"임을 명시하는 것이 안전합니다.

---

## 10. 전체 공식 요약표

| 번호 | 공식 이름 | 어디에 쓰이나 |
|---|---|---|
| 1 | 기본 운동방정식 | 모든 시뮬레이션의 출발점 |
| 2 | 고유진동수 / 감쇠비 / 감쇠고유진동수 | 시스템 성격 파악 |
| 3 | 로그감쇠율 | 실물 모형의 m,c,k 실험적 추정 |
| 4 | 이동하중 함수 | 차량 통행 재현 (외력 입력) |
| 5.1 | 안티페이즈 목표식 | 노이즈캔슬링식 제어 개념 |
| 5.2 | LMS 적응 필터 | 안티페이즈의 실제 구현 (고급) |
| 5.3 | PID 제어 | 제어력 계산 (우선 구현 권장) |
| 6 | 2자유도(2-DOF) 시스템 | AMD(무게추) 확장 모델 |
| 7 | 상태공간 모델 / LQR | 고도화된 최적 제어 (심화, 선택) |
| 8 | 전달함수 / 진동전달률 | 주파수 분석, 승차감 지표 |
| 9 | 저감률 / 정착시간 / 수명비 | 최종 성능 평가 지표 |

## 11. Simulink 구현 순서 제안

1. **1장의 개방루프 모델** 구축 (Transfer Function 블록 또는 Simscape 사용)
2. **3장의 로그감쇠율 공식**으로 실측 데이터에서 m, c, k 역산 → 시뮬레이션에 대입해 검증
3. **4장의 이동하중**을 외력 입력으로 구성
4. **5.3장의 PID**로 폐루프 제어 추가 (가장 먼저 구현할 것)
5. 여유가 되면 **6장(2-DOF)**으로 AMD 확장, **7장(LQR)**으로 고도화
6. **9장의 지표**로 시뮬레이션과 실측 결과를 정량 비교
